"use strict";
var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var fs_1 = __importDefault(require("fs"));
var path_1 = require("path");
var getDireflowConfig = function (indexPath) {
    try {
        var paths = indexPath.split(path_1.sep);
        var rootPath = __spreadArrays(paths).slice(0, paths.length - 2).join(path_1.sep);
        var config = fs_1.default.readFileSync(rootPath + "/direflow-config.json", 'utf8');
        if (!config) {
            throw Error();
        }
        return JSON.parse(config);
    }
    catch (error) { /* Suppress error */ }
};
exports.default = getDireflowConfig;
